on: [push]
name: newtmptask1



jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      ResourceGroupName: deep123
      ResourceGroupLocation: "eastus"
    steps:
      - uses: actions/checkout@master
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
         
      - uses: Azure/CLI@v1
        with:
          inlineScript: |
            #!/bin/bash
            az get-credentials --resource-group deep123 --name muffin25
